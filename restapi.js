//Restfull api :- \
// 1.Server and client arch:the server and client are independent and they should not depend on each other.
//Server sends the data in html form(SSR) that if your client is a browser or it can read the html then its oky and fast
//but if u had like mobile or ios and want the data there so we send it in JSON to the client.

//2.Always respect all http Methods.
//GET/USER: users data read kro and return the data.
//Post/user: handle new user creation.
//patch/user:update the user.

// we have to 

